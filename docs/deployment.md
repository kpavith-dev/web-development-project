# Deployment Guide

## Frontend (Vercel)
1. Build the frontend with `npm run build`.
2. Deploy the generated dist folder to Vercel.

## Backend (Render)
1. Create a Node.js service on Render.
2. Set environment variables from server/.env.
3. Deploy the server directory.

## Database
- Use MongoDB Atlas and set MONGO_URI accordingly.
